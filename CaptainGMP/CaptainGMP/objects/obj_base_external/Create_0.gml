/// @description Ini vars

// Inherit the parent event
event_inherited();

// this is the ancestor of all external components, external means on the outside of the ship, e.g. turrets

name = "Base External Component";