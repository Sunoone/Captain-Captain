/// @description Ini vars

// Inherit the parent event
event_inherited();

// Make changes here

type = 2; // declare as component


	// internal variuables
target_id = -4;
clock_speed = 1;
bonus_clock = 0;
max_hack_level = 1;