/// @description scr_stat_get_array( obj, percentage )
/// @param obj
/// @param percentage

// this script fetches the right stat array

if( argument1 )
	return argument0.stat_p;
else
	return argument0.stat;